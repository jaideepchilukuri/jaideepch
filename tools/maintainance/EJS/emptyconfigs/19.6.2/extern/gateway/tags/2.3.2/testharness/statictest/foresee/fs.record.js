_fsRequire(["fs", "$fs.utils.js", "$fs.trigger.js"], function(fs, utils, trigger) {

  console.warn("FS RECORD INIT in 19.9.0 which needed fs: ", fs, " AND utils: ", utils, " AND TRIGGER: ", trigger);

  return {
    iam: "record"
  };
});